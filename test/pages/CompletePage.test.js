import { fireEvent, getByText } from "@testing-library/dom";
import "@testing-library/jest-dom/extend-expect";
import { JSDOM } from "jsdom";

describe("게임 완료 페이지 테스트", () => {
  test("query로 전달한 점수 & 평균 시간 화면에 표시", async () => {});
});
